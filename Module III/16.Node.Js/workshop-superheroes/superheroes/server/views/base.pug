doctype
html
    head
        meta(charset="utf-8")
        title superheroes
    body
        header 
            h3 Some navigation
                ul
                if !user
                    li
                        a(href="/auth/login") Login
                    li
                        a(href="/auth/register") Register
                else
                    li
                        a(href="/auth/logout") Logout 
        main
        block content